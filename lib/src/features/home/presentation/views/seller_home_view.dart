import 'package:flutter/material.dart';

class SellerMainView extends StatelessWidget {
  const SellerMainView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: AppBar(
        title: const Text('Seller Home View'),
      ),
    );
  }
}
